package A2_Q1;

public class Question_1 {
	// class person:
	// 		String familyName;
	//		String givenName;
	
	
	/**
	 * uses the compareTo Method to compare two names lexicographically.
	 * compares the familyNames and
	 * return integer to indicate if greater/equal to/less than argument.
	 * 
	 * 
	 * if familyNames are the same, it will compare the givenNames and 
	 * return integer to indicate if greater/equal to/less than argument.
	 * 
	 * 
	 * @param per: instance of Person Object. 
	 * 		Person object contains familyName and givenName data fields.
	 * @return int result of comparison:
	 * 		result = 0  if both strings are equal
	 * 		result < 0  if string is less than argument
	 * 		result > 0  if string is bigger than argument
	 */
	
	
//	public int compareTo(Person per) {
//		if (familyName.compareTo(per.familyName) == 0)
//		return givenName.compareTo(per.givenName);
//		else
//		return familyName.compareTo(per.familyName)
//		}
	
	
}
